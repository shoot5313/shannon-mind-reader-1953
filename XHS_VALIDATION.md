# 小红书小工具上传包校验摘要

校验日期：2026-09-01（1.1.0 玩法更新后重跑）
目标版本：香农读心机 `1.1.0`
规范来源：官方 `minitool-zip-builder 1.6.0`，安装于 `.codex/minitool-zip-builder/`。

## 最终产物

- 上传 ZIP：`dist/shannon-mind-reader-v1.1.0.zip`
- ZIP 大小：`461893` bytes（约 `0.44 MiB`）
- ZIP SHA-256：`674a341e9f70769acf35e678083d6f057eb1b8b20722f1def508c81a85a85077`
- 平台图标：`dist/shannon-mind-reader-icon-1024.png`
- 图标 SHA-256：`66b5a7be9fd5399b82394b88358e5f5beebe8a4fb1c734ac94549b2a8c484240`

## 规范校验

- [x] ZIP 根目录存在且仅存在一个 `index.html`，未多套目录。
- [x] 11 个运行文件全部属于允许类型；无 `node_modules`、`.git`、source map、构建配置或隐藏垃圾文件。
- [x] ZIP 完整性检查通过，全部 8 个 HTML 静态资源引用均存在且使用相对路径。
- [x] 无自建 CSP meta、PWA manifest、内联脚本、行内事件、module/import/export、iframe/object。
- [x] 无网络请求、外部图片/字体/脚本、Worker、WASM、定位、剪贴板、传感器、WebRTC、窗口或下载 API。
- [x] 发布 JS 已收敛到 ES2017：无可选链、空值合并、逻辑赋值、对象展开、`replaceAll`、`Array.at` 等 Chrome 61 门槛外写法。
- [x] CSS 已增加 Chrome 61 基线层：Grid 使用 `grid-gap` 回退；Flex 间距经实际布局测量后才启用 `gap`；`min/max/clamp`、`aspect-ratio`、`inset`、动态视口与 `:focus-visible` 均有局部回退。
- [x] 大厅以冒险为主入口，同时提供次级的 `CASE 8` 实验人员通道；航行结算页仍保留未登记档案入口。研究室独立采用 `8 × 8 = 64` 手，并提供不离开研究室的本局重置。
- [x] CASE 8 的八格访问次数来自本局真实状态记录；前两手不伪造状态，结算固定汇总 `62 / 62` 次有效翻阅，并在页面与 Canvas 战报中绘制八格访问谱。
- [x] PC 模拟器安全区变量与真机 `env()` 均有兼容写法；390×844 视口无横向溢出。
- [x] 结果卡为本地 Canvas 生成；按官方契约调用 `writeTempFile` → `saveImageToPhotosAlbum`。
- [x] Bridge 模拟验收：完整 PNG data URI、单次临时文件写入、单次相册保存、图片尺寸 `1080×1440`。
- [x] 页面加载性能记录中外部网络资源数为 `0`。
- [x] 单元/模拟/发布测试全部通过；16 种结果与冒险结局回归 `16/16`，覆盖三档宝藏图鉴、被捕结局、匿名进入、大厅直达 `CASE 8`、航行结算入口和无模式切换。
- [x] 首次上手与回合反馈浏览器回归通过：第一手选择前光束关闭、选择后正常显形；规则因果清楚、结果在按钮解锁后仍保留；海况在第 0 / 30 / 120 手分别为平海、风浪、暴风，30/60/90/120 海里文字节点可见，掉命信息完整；CASE 8 第 8 手切入第 2 组，重置后回到 `00/64`、第 1 组与全空记忆。
- [x] 1.6.0 官方 Python 审计脚本分别检查解压目录与最终 ZIP：均为 `PASS: 11 file(s), 0 warning(s)`。
- [x] 解压后最大文本文件约 `59 KiB`、文本合计约 `161 KiB`；无静态 Base64 大资源、无 WebGL，远低于性能风险线。

## 实测边界

- 当前无头 Chrome 的 390×844 手机视口、完整交互与最终 ZIP 解压入口已经实测。
- 未在 Android 8.1 / Chrome 61 旧内核上直接运行，因此结论是“Chrome 61 静态兼容门禁通过”，不是“旧内核真机已实测”。
- 未采集低端真机帧率、峰值内存或首屏耗时；包体与代码设计门禁通过，但不宣称已完成真机性能测量。

## 权限

不使用相机、麦克风、定位、通知、账号或剪贴板权限。只有玩家主动点击“保存到相册”时，系统才可能请求相册权限。

## 上传后真机冒烟

平台注入与系统授权弹窗无法在普通浏览器中完全复现。上传预览后只需在小红书真机点一次“保存到相册”，确认首次相册授权和成功提示；拒绝授权时页面已有失败提示与系统截图兜底。
