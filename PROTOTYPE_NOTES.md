# Escape v2 玩法原型

## 第一轮结论（已完成）

- 比较：藏硬币 / 猜拳 / 逃脱。
- 选择：逃脱。
- 原因：玩家感受到的是“别被它抓到”，每次命中都有现场事件；代入感明显强于抽象随机选择。
- 从藏硬币保留：1953 装置语言、预测先封存、实体道具和结果证据。
- 删除：猜拳主玩法。它会把产品从“当场可验证的读心实验”改成普通人机对战。

## 第二轮要回答的问题

什么能让最多 150 次二元选择持续像一场比拼，并让最后的统计揭露从“哦”变成“卧槽”？

- `?variant=escape-beam`：纯光束追逐。探照灯扑向机器事先封存的方向；命中时锁红，猜错时机器明确认错。
- `?variant=escape-blackout`：三枚真硬币。三次由本机随机源替玩家选路，测试有限道具是否能产生真正的策略感，又不拖慢每回合循环。
- `?variant=escape-trace`：活路线。150 格路线从第一步就是游戏板；机器只在 090 / 120 / 150 三个预注册检查点查看证据，足够强才提前说“我知道了”。

共同机制：

- 探照灯只泄露机器把握的强弱，不泄露方向；显示定性强度而非未经校准的假精确百分比。
- 前 10 回合反馈较慢，之后加速；第 30、70、110 回合进入新阶段。
- 机器每次猜错都明确说“我错了”。音效与振动只用本地 Web API，缺失时静默降级。
- 结算不再用一句话替玩家下结论。路线逐格扫描，所有符合预注册条件的位置依次点亮，同时显示证据与反例。
- 真硬币局不进入“你个人的破绽”检验，避免把外部随机选择算成人类习惯。

## 为什么没有直接采用逐局押注

押注会把一次按键变成两次操作，150 回合很容易变成苦役。如果玩家先看到机器置信度再决定押注，“高注局更容易猜”也会和机器本来就在高置信回合更准混在一起。等核心循环成立后，可以单独做一版一手势押注实验。

## 为什么不能每一步 `p < 0.05` 就收官

连续反复检验会抬高假阳性。当前提前收官版只在 090 / 120 / 150 检查，并使用 Bonferroni 校正阈值 `0.05 / 3 = 0.0167`；其余两版固定走到 150。

## 怎么运行

```sh
npm run serve
```

打开：

- `http://localhost:4173/?variant=escape-beam`
- `http://localhost:4173/?variant=escape-blackout`
- `http://localhost:4173/?variant=escape-trace`

加上 `&scene=result` 可以直接检查完整路线的结算态。底部切换器或键盘左右键可以切换版本。

## 第二轮 Verdict

- 选择：`escape-beam`。光束现场揭晓比静态路线更有对手感。
- 从 B 保留：三枚真硬币的世界观与“你可以三次停止做自己”；不作为默认逐局操作。
- 从 C 保留：路线就是证据；结算时回放，不把 150 格同时当装饰。
- 新风险：原 0–5 阶混合器只看左右序列，不看玩家刚刚被抓还是脱身。Escape 把反馈变成了策略的一部分，模型却没有同步升级。

## 第三轮：代码诊断与 Shannon 反转

两次人工结果 52% / 46% 不能单独证明实现错误。确定性探针确认：

- 预测在选择前封存，`observe` 没有偷看当前选择。
- 浏览器循环与直接调用引擎逐格一致。
- 固定周期在第 31–150 格可达到 100% 命中。
- 改随机种子对固定 150 格路线只造成约 ±0.33 个百分点，解释不了 52% 与 46%。

真正的边界是反馈。模拟“被抓就换、脱身就留”时：

- 原 0–5 阶混合器：57.1%。
- Shannon 1953 八记忆格：80.2%。

八格算法恰好把前两局输赢和上一局留/换纳入状态，也保留了原文所述“玩家可以反过来研究机器”的性质。自动维护八格状态的测试玩家在 150 格平均把机器压到 27.4%，20.9% 的会话达到隐藏成就线；具体玩家攻略不进入公开界面文案。

因此 Escape 已切换为真正的八格机器，并加入两种互补终局：

- ACT II / III / IV 各管一只封锁继电器；每幕连续三次按记忆命中才落下本幕的一只，三幕全部失守会提前判负。模拟中真随机误杀 5.2%，普通一阶偏差 13.7%，反馈反应型 99.0%。
- 走满 150 格且脱身次数严格大于命中次数三倍，获得 3:1 大奖。大奖证据回放只点亮机器按记忆出手的格子。

封锁是游戏输赢，不冒充统计显著性；结果页会把两者明确分开。盲测使用 `PILOT_PROTOCOL.md`，并通过 `experiments/analyse-pilot.cjs` 按种子逐格重放。

## 第四轮：一台机器的两层挑战

内部保留两套循环：

- `?variant=adventure`：30 格后启用追踪；三次连续的 Shannon 记忆命中熄灭一盏命灯，累计三盏立即被捕。走满 150 格发宝藏海图。
- `?variant=duel`：固定 8 组 × 8 手。玩家观察八只记忆格自己探索玩法；每组封存后点亮一格档案灯。界面不显示格子学到的是留还是换，也不直接教授反制策略。

冒险的三条命采用跨全程累计，而非旧版每幕最多一条。Shannon 模拟中，真随机路线被捕约 21.2%，典型一阶偏差约 41.4%，反馈反应型 100%；它比旧版更危险，但仍不会把机器的盲猜算作红色追踪。

对弈称号不再伪装成连续量表，所有结果都是一枚完整贴纸：

- 玩家领先 → `100% 聪明蛋`
- `32:32` → `100% 普通蛋`
- 机器领先 → `100% 笨蛋`
- `49:15` → `100% 聪明蛋` + 隐藏章
- `15:49` → `100% 大坏蛋` + 隐藏章

两条极端线仍作为引擎里的对称隐藏成就；达到时在基础称号之外加盖一枚章。

## 第五轮：统一实验大厅与实名战报

默认路由改为一条完整叙事：1953 贝尔实验室档案 → 可选本地昵称 → 直接进入冒险。一次航行结束后才开放八格研究室。视觉采用绿色显像管、黑色电木、琥珀继电器与打孔纸带；借鉴终端式游戏的全屏 CRT 和硬边工作台语言，但不使用蓝紫霓虹，也不依赖 WebGL。

昵称最多保留 12 个 Unicode 字符，通过 URL 参数传递，不写 `localStorage`。引擎统一生成三类社交文案：

- `L，恭喜你成为香农严选 100% 聪明蛋，你可以用智力发电！`
- `R，你是香农严选 100% 笨蛋，不过傻人有傻福，你可以用运气发电！`
- `Y，恭喜你拿到香农送出的宝藏，请扬帆启航，走上人生巅峰！`

所有终局增加 1080 × 1440 Canvas 战报：对弈卡写入昵称、蛋称号与实际比分；冒险卡写入昵称、命灯/终止海里与藏宝路线。生成过程完全本机执行，预览可长按保存，并提供普通 PNG 下载作为桌面回退。

## 第六轮：两种模式共用一台机器

博弈版不再使用独立的灰色机箱与米色结果纸，统一接入冒险版的深海显像管视觉：`#061923` 深海底、`#76f7b0` 磷光信号、`#ef665c` 截获警报、`#ffb84a` 大奖琥珀。开场、八格控制舱、结算页和 Canvas 分享卡使用同一组语义色。

蛋保留为模式识别物，但改成带扫描线与辉光的真空管：普通蛋为磷光绿、聪明蛋为琥珀金、笨蛋为氧化铜绿、大坏蛋为警报红。它是这套视觉唯一刻意放大的元素，其余结构和交互位置不变。

## 第七轮：把决策还给玩家

前六轮都在打磨"机器读你"这件事本身，没检查过玩家在这 150 次按键里做了什么决策。答案是：零。用引擎重跑各类策略（`experiments/tune-adventure.cjs`，每档 15000 局）暴露了四个问题。

### 主导策略是不参与

150 手 / 热身 30 的旧配置下：

| 策略 | 被捕 | 3 灯 |
| --- | --- | --- |
| 纯随机 | 20.2% | 24.8% |
| 避免连三同 | 46.8% | 10.3% |
| 反应型人类 | 91.8% | 0.4% |
| 完美利用者 | 0.0% | 100% |

抛硬币的人活得最好，最稀有的宝藏发给最不动脑的玩家。而完美利用者（在脑内镜像机器的状态机——它的输入全是公开信息）能做到零被捕，说明真解存在，但获取它需要跨局手工维护八个锁存器。结果是群体被劈成"随机最优"和"已破解"两段，中间没有台阶。

### 隐藏成就阈值超出理论上限

`49:15`（3:1）要求机器 64 手只中 ≤15。完美玩法的实测分布是中位数 24、p5 = 18，达成率 **1.0%**——它是彩票不是成就。阈值原本按 150 手调，研究室收成 64 手后没跟着重标：样本量减半而方差没变，尾部就够不着了。

改成 2:1（机器 ≤21）。达成率：随机 0.4% / 避免连三同 0.1% / 半熟练 6.4% / 完美 26.0%。运气摸不到，真技巧四局中一次，中间多了一级可见台阶。对称的"大坏蛋"侧同步改成机器 ≥ 2×玩家，反应型人类 13.3%——这枚章本来就该发给真的被读穿的人。用比例而非常数，是为了下次再改手数时它自己跟着走。

### 缩短必须配风暴

把 150→100、热身 30→10 之后随机被捕率掉到 9.4%、3 灯率涨到 40.1%，稀有度阶梯直接倒转。补偿手段是让"大浪滔天"真正生效：第 80 手起追踪链从 3 收到 2。原本 120 手后的风暴只是天气美术，没有任何机制变化。

最终 100 手 / 热身 10 / 风暴@80 / 3 枚硬币：随机被捕 27.3%、3 灯 19.7%、2 灯 28.3%。比旧配置略难，阶梯更干净，首次真实代价从第 31 手提前到第 11 手，整局从 4~5 分钟压到 2~3 分钟。规则收紧的那一手必须明说，否则是黑箱惩罚。

### 三枚硬币回归

第二轮 `escape-blackout` 原型过真硬币，当时以"不能做默认逐局操作"被砍——那个判断是对的。现在它作为限量资源回来：三枚，按下由本机 CSPRNG 替你走一手。每回合从"按左还是右"（0 决策）变成"这一手花不花"，且主题贴合。它不是免疫：机器照常先押、照常可能命中，硬币只是把受训格的接近 100% 降回 50%。

硬币手不进入习惯检验（沿用第二轮定下的原则），实现上是丢掉所有读到硬币手的窗口，而不是只丢那一手——否则玩家可以用硬币洗掉自己的破绽。

### 结算揭示

`analyseSwitching` / `classifyResult` 三个月前就写好并测好了，但 `grep` 显示只被 `tests/` 引用，两个上线模式都没调用。玩家掉灯时只知道"被连抓三次"，永远不知道是哪个习惯出卖了自己——恰好是最该给出去的那句话。

现在两个模式的结算都调用它，并且三个分支都诚实：抓到就报换边率、机会数和 p 值；没抓到就明说"没有统计意义上的破绽"；机会数不足 20 就明说不下结论（第 19 手最早被捕的那种局只有 6 次机会，不该硬凑结论）。

保密立场不变：只揭示玩家自己的统计习惯，不揭示八格里存的是"留"还是"换"。八格访问谱补了一行"最容易被读穿"，报的是玩家在该格的被抓率——同样是玩家自己的透明度。机器一次都没抓到的玩家不会被硬安一个"最差格"。

## 第八轮：把探照灯接上

第七轮把结局修好了，循环没修。实测复盘：100 手里有 97 手玩家没有任何可依据的信息，主导策略仍然是"别做你自己"。而第七轮加的三枚硬币，实测只值 4 个百分点（随机玩家 0），是个安慰剂——它给了能动性的*感觉*，没给决策。

翻回第二轮笔记，规则本来就写好了：

> 探照灯只泄露机器把握的强弱，不泄露方向；显示定性强度而非未经校准的假精确百分比。

`grep` 结果：冒险模式从头到尾没读过 `state.pending.trained`。这条设计意图写下来了，从来没接上——和 `analyseSwitching` 一模一样的情况。

### 接上之后

机器每一手都知道自己是在调用已学会的记忆格，还是在瞎猜。现在灯塔说出来：亮灯=`它认得这个局面`，暗灯=`它没见过这个局面`。方向仍然封存到出手之后。

实测（每档 20000 局，普通"避免连三同"习惯）：

| 策略 | 被捕 | 3 灯 |
| --- | --- | --- |
| 纯随机 | 27.7% | 19.6% |
| 有习惯 · 不看灯 | 50.1% | 9.3% |
| 有习惯 · 亮灯就抛硬币 | 27.1% | 19.7% |
| **有习惯 · 亮灯就反直觉** | **26.1%** | 24.1% |
| 反应型 · 亮灯就反直觉 | 14.4% | 47.6% |
| 完美利用者（上限） | 0.0% | 100% |

**主导策略第一次不再是抛硬币。**认真玩（26.1%）赢过随机（27.7%），不看灯的习惯玩家（50.1%）输给随机。而且"亮灯就随机"只到 27.1%——光看到灯不够，得真的反自己的直觉，所以技能梯度是连续的而不是开关。

更漂亮的一点：越可读的玩家从灯里赚得越多（反应型 90.8% → 14.4%）。灯把"你的可预测性"从弱点变成武器，这正是 1953 原文的论点。

### 信号密度

亮灯率按 10 手分桶：3% → 24% → 40% → 45% → 47% → 稳定在 50%。相邻两手翻转率 44%。

前 10 手几乎全暗，因为机器确实还没见过任何局面——热身段第一次有了正当理由：**这是在零风险下学会看灯的地方**，而不是空转的 10 手。中段自然形成"机器逐渐醒来"的叙事弧，末段稳定在 50% —— 信息量最大，且必须每手重看。

### 没有泄露方向

`state.pending.choice` 不允许出现在渲染路径上，由 `tests/release.test.cjs` 剥掉注释后断言；浏览器回归还会检查出手前 `.sea-arena` 不带 `beam-left/right`，且读数文案里不出现"它押左/右"。锥形光束在两种状态下都左右扫，只有亮度和频率不同。

### 硬币被砍掉了

灯接上之后重测硬币：看灯玩家 26.3%（不用币）→ 24.6%（最优用币），1.7 个百分点。原因是**反直觉比抛硬币更强**——在亮灯手上，一个知道自己习惯的玩家反着来，胜过把这一手交给 50/50。硬币和更好的工具竞争，且输了。

所以砍掉。一个 3 分钟的游戏里，一个清晰的动词胜过两个（其中一个是安慰剂）。硬币占着一个按钮、一行简报文案、一处结算文案和一段战报文字，全都在和那盏灯抢注意力。

连带删掉的还有 `analyseSwitching` 的 `ignoreIndices`——它存在的唯一理由是"真硬币局不进入你个人的破绽检验"（第二轮定的原则）。原则本身仍然成立：**任何不由玩家选择的手都不该算进习惯检验**。如果以后再引入这类机制，要连同排除窗口一起加回来，而且要丢掉所有读到该手的窗口，不能只丢那一手——否则玩家能用它洗掉自己的破绽。

这也是第七轮的教训：我在没有测量的情况下宣称硬币"把 0 决策变成真决策"。实际是 4 个百分点（随机玩家 0）。先量再说。

## 第九轮：行为称号

第一份真人反馈：一位朋友玩完说**看了灯、游戏性强不少、更刺激**。这是第八轮那盏灯唯一的外部证据（n=1）。但「玩家会自发比较八格」没有被验证——footprint 的差异是产品方发现的，不是玩家。她提的需求是「独特性格标签」。

### 最热格是运气，不是性格

直觉做法是拿访问谱最热的那一格当性格。实测否掉了：

| 行为 | 最热格分布 |
| --- | --- |
| 爱留 | `输·留·输` 99% |
| 爱换 | `输·换·输` 44% ／ `赢·换·输` 43% |
| 纯随机 | 四格各约 17% |

八格索引是 `[输赢]·[留换]·[输赢]`。**中间那个字是行为，外面两个字是结果。**同样爱换的人，最热格在输/赢之间五五开，纯看运气。「你是赢留赢、朋友是输留输」多半只是同样爱留、你赢他输。拿它当性格就是占星术。

### 四个真能测的轴

改成测八格所索引的那些量，每个轴都带显著性检验和「未测出」状态：

| 轴 | 检验 | 假阳性 |
| --- | --- | --- |
| ① 留 / 换 | `analyseSwitching`（已有） | ~4% |
| ② 亮灯时是否改打法 | Fisher 精确检验 | 4.0% |
| ③ 左 / 右偏好 | 自相关校正二项检验 | 4.8% |
| ④ 输赢是否改变你 | Fisher 精确检验 | 4.5% |

用 Fisher 精确检验而非正态近似，理由和 `binomialTwoSided` 一样：这些检验决定机器对一个人下什么结论，而样本量小到近似会在最容易误判的地方失准。

### 轴③ 差点变成占星术

未校正的左右偏好检验，对「爱留但无左右偏好」的玩家给出 **48% 假阳性**——长连段违反了二项检验的独立性假设，把随机起手的那一边放大成「偏好」。按一阶自相关折算有效样本量后降到 3.8%：100 手里只做了约 14 次独立决定的黏人玩家，就按 14 检验。

`tests/simulation.test.cjs` 里那条黏人玩家的断言就是守这个的。

### 读灯玩家一度全被叫「掌灯人」

读灯 = 亮灯时反直觉，原始留换序列被搅乱，轴① 检出率从 100% 掉到 34%，于是所有读灯玩家都退化成同一个名号——和「独特」正好相反。兜底：主检验失败时，只用**灯暗**的窗口重测（alpha 收紧到 0.02 补偿二次检验）。「爱留·读灯」从 34% 回到 87%，而纯随机玩家的假阳性只增加 0.1 个百分点。

这一步用回了砍硬币时删掉的 `analyseSwitching({ ignoreIndices })`——当时删是对的（没有调用方），现在它有真实用途了。

### 蛋和称号不冲突

蛋是**结果**，称号是**行为**。它们不独立，但也不重复：

- 钉子户 **不读灯** → 大坏蛋 85%
- 钉子户 **读灯** → 聪明蛋 94%

**那盏灯就是解耦器。**文案分工：称号说「你是谁」，蛋说「代价是什么」——不是骂两遍，是因果。

界面上一度真的说了两遍：旧的破绽报告和称号的轴① 是同一句话。破绽报告已并入称号卡。

### 两张卡

蛋卡和称号卡分开，屏幕上称号默认折叠、想深入的人再展开。挤在一张卡里会撑爆版面（实测和访问谱重叠），分开之后称号卡能把四个轴全部铺开。

四行永远都在，亮的带证据、暗的写「未测出」——两个人的卡片并排，能直接读出机器读到了谁的哪几项。这正是最初那个「我和朋友正好相反」的场景。
