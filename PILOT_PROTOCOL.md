# 读心机盲测协议

目的：区分三件事——浏览器是否按预期执行、1953 八格模型是否抓到真实玩家、玩家是否会根据上一束光改变下一步。

## 给组织者

1. 启动本地页面：`npm run serve`。
2. 每位参与者使用不同编号，例如：
   - `http://localhost:4173/?pilot=1&pid=P01&locks=off`
   - `http://localhost:4173/?pilot=1&pid=P02&locks=off`
3. 每人只玩一局，不围观前一位的结果，不在中途指导。
4. 结束后点击“导出匿名盲测数据”，保存 JSON。
5. 另记一句原话：“你觉得机器从什么时候开始像在猜你？”不要追问原因。

`locks=off` 只关闭提前判负，保证诊断局完整收集 150 格；预测器、灯光与逐局反馈不变。

## 只对参与者说

> 这是一个左右逃脱小游戏。每个路口选左或右，尽量别被探照灯抓到。走完后把结果页给我。

不要提前提这些词：随机、人类偏差、换边、八格、算法、命中率、3:1、复仇局。

## 样本量解释

- 3 人：只够发现操作问题和明显故障。
- 10 人：可以判断“机器一直像瞎猜”是否普遍。
- 30 人以上：才开始适合讨论命中率或结局分布；仍应报告原始分布，不把模拟百分位说成真人百分位。

## 本地核验

把所有 JSON 放进一个目录后逐个列出文件运行：

```bash
node experiments/analyse-pilot.cjs pilot/P01-*.json pilot/P02-*.json
```

“逐格重放”必须全部为 `PASS`。任何不一致都先按实现错误处理，不解释成玩家行为。

导出文件只含按键、机器已封存的预测、八格状态、输赢和检验结果，不含姓名、设备标识或网络信息。
